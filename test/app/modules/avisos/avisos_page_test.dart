import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:flutter_modular/flutter_modular_test.dart';

import 'package:fatec_estacionamento/app/modules/avisos/avisos_page.dart';

void main() {
  testWidgets('AvisosPage has title', (tester) async {
    //  await tester.pumpWidget(buildTestableWidget(AvisosPage(title: 'Avisos')));
    //  final titleFinder = find.text('Avisos');
    //  expect(titleFinder, findsOneWidget);
  });
}
