import 'package:flutter_modular/flutter_modular_test.dart';
import 'package:flutter_test/flutter_test.dart';

import 'package:fatec_estacionamento/app/app_module.dart';

void main() {
  initModule(AppModule());
  // VeiculoCardController veiculocard;
  //
  setUp(() {
    //     veiculocard = AppModule.to.get<VeiculoCardController>();
  });

  group('VeiculoCardController Test', () {
    //   test("First Test", () {
    //     expect(veiculocard, isInstanceOf<VeiculoCardController>());
    //   });

    //   test("Set Value", () {
    //     expect(veiculocard.value, equals(0));
    //     veiculocard.increment();
    //     expect(veiculocard.value, equals(1));
    //   });
  });
}
